// Generated by CoffeeScript 2.6.1
(function() {
  // Temporary Channel
  var Channel, TemporaryChannel, debug,
    boundMethodCheck = function(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new Error('Bound instance method accessed before binding'); } };

  debug = require('./config').debug('amqp:TemporaryChannel');

  Channel = require('./Channel');

  // This is just a skeleton of a simple channel object to pass around
  TemporaryChannel = class TemporaryChannel extends Channel {
    constructor(connection, channel, cb) {
      super(connection, channel);
      this._channelOpen = this._channelOpen.bind(this);
      this.cb = cb;
      this.temporaryChannel();
      return this;
    }

    _channelOpen() {
      boundMethodCheck(this, TemporaryChannel);
      if (this.cb != null) {
        this.cb(null, this);
      }
      return this.cb = null;
    }

    _channelClosed() {}

    // do nothing
    _onMethod() {}

  };

  // do nothing
  module.exports = TemporaryChannel;

}).call(this);
