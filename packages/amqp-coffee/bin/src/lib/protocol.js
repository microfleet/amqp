// Generated by CoffeeScript 2.6.1
(function() {
  module.exports = function(protocolFile) {
    var classInfo, classes, i, j, len, len1, method, methodInfo, methodTable, methods, name, protocol, ref, ref1;
    protocol = require(protocolFile);
    methodTable = {};
    methods = {};
    classes = {};
    ref = protocol.classes;
    for (i = 0, len = ref.length; i < len; i++) {
      classInfo = ref[i];
      classes[classInfo.index] = classInfo;
      ref1 = classInfo.methods;
      for (j = 0, len1 = ref1.length; j < len1; j++) {
        methodInfo = ref1[j];
        // className + methodInfo.name.toCammelCase
        name = `${classInfo.name}${methodInfo.name[0].toUpperCase()}${methodInfo.name.slice(1)}`;
        method = {
          name,
          fields: methodInfo.fields,
          methodIndex: methodInfo.index,
          classIndex: classInfo.index
        };
        if (methodTable[method.classIndex] == null) {
          methodTable[method.classIndex] = {};
        }
        methodTable[method.classIndex][method.methodIndex] = method;
        methods[name] = method;
      }
    }
    return {methods, methodTable, classes};
  };

}).call(this);
